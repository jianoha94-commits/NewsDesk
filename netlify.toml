# Netlify 미러 배포 설정 (정적 사이트, 빌드 불필요)
# 사용법: netlify.com 에서 이 저장소를 연결하면 자동 배포됩니다.

[build]
  publish = "."
  command = ""

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "no-referrer"
    X-Frame-Options = "SAMEORIGIN"
